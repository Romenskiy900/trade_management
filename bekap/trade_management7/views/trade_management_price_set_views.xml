<odoo>

    <record id="view_trade_management_price_set_list" model="ir.ui.view">
        <field name="name">trade.management.price.set.list</field>
        <field name="model">trade.management.price.set</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="number"/>
                <field name="date"/>
            </list>
        </field>
    </record>

    <record id="view_trade_management_price_set_form" model="ir.ui.view">

        <field name="name">trade.management.price.set.form</field>
        <field name="model">trade.management.price.set</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="number"/>
                        <field name="date"/>
                    </group>

                    <notebook>

                        <page string="Товари">

                            <field name="line_ids">

                                <list editable="bottom">
                                    <field name="product_id"/>
                                    <field name="price"/>
                                </list>

                            </field>

                        </page>

                    </notebook>

                </sheet>
            </form>
        </field>
    </record>

    <record id="action_trade_management_price_set" model="ir.actions.act_window">
        <field name="name">Встановлення цін</field>
        <field name="res_model">trade.management.price.set</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>