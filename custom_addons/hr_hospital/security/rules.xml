<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="rule_patient_own" model="ir.rule">
            <field name="name">Patient: own record only</field>
            <field name="model_id" ref="model_hr_hospital_patient"/>
            <field name="groups" eval="[(4, ref('hr_hospital.group_patient'))]"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
        </record>

    </data>
</odoo>